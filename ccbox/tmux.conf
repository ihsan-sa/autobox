# inner tmux (inside ccbox containers): prefix C-a so it does not collide with the host tmux's C-b
set -g prefix C-a
unbind C-b
bind C-a send-prefix
set -g mouse on
set -g history-limit 50000
set -s escape-time 10
set -g default-terminal "tmux-256color"
set -g status-right "ccbox #H  %H:%M"
