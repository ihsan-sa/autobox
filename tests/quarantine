# Cases a landing does not go red for — core/tests/green.sh, QUARANTINE, has the rule. One per line:
# <tool><TAB><text the red line carries><TAB><why>. They still run and still report.
cc-guard	a refusal that fell on words is recorded once	50a: flips under load, green alone (three retained logs, 2026-09)
cc-broker	an inject arriving while a send is hanging is written down at once	timing against a hanging flush; green alone
cc-native	claim/checkpoint deadline expired	the 11 s deadline expires under load and the selfcheck dies with no tally
cc-member-import	deadline refuses at	asserts a wall-clock bound on the process deadline; slow under load
cc-sandbox	DURING the launch never reaches the mount	the swap race: a sleep against a launch that is slower under load
cc-limit	relative reset parsed to	inline in selftest (qbad); the brief calls it cc-model's, but this suite line is the one that prints it (+71s/+72s on #636, #637). Main pins its clock (CC_LIMIT_NOW) since; listed so a regression under load still reports
cc	a repair dispatch was refused a row a subagent still holds	inline in selftest (qbad): went red once on a PR touching only the slot lock; green alone
