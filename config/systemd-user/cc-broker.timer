[Unit]
Description=cc-broker flush every 60 s (a held event must never wait longer than its debounce, and a delivery nobody took is retried here)
[Timer]
OnBootSec=2min
OnUnitActiveSec=60s
Persistent=false
[Install]
WantedBy=timers.target
