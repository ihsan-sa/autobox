[Unit]
Description=cc-msg drain every 60 s (a message queued behind a dialog must not wait for the next thing to happen)
[Timer]
OnBootSec=2min
OnUnitActiveSec=60s
Persistent=false
[Install]
WantedBy=timers.target
