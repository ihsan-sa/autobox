[Unit]
Description=Daily cc-audit auto (03:30 UTC — picks review / weekly / monthly / arch, else the suites and the main-gate row)
[Timer]
OnCalendar=*-*-* 03:30:00 UTC
Persistent=true
[Install]
WantedBy=timers.target
