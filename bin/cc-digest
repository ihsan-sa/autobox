#!/usr/bin/env python3
"""cc-digest — gone. The owner's daily line is `cc-reconcile digest`; this name only answers because cc-slack's
`!digest` still execs it (track shrink-slack-1 repoints that call, and then this file goes with it)."""
print("cc-digest is gone — the daily line is `cc-reconcile digest` (`!digest` is being repointed, track shrink-slack-1).")
