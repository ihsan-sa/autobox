# Key-only SSH. Sorts before 50-cloud-init.conf, and sshd uses the first value
# obtained, so this overrides cloud-init's "yes". install.sh --etc runs `sshd -t`
# and restores the previous file if this one is rejected.
PasswordAuthentication no
KbdInteractiveAuthentication no
