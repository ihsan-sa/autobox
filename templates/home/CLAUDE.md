# <box> — <make/model>, headless home server

- Hardware: <cpu>, <ram>, <disk> `<device>` (<filesystem layout>), <ethernet driver> ethernet `<iface>` (MAC `<mac>`), <wifi adapter> `<wlan-iface>` (<driver>). Service tag/serial `<serial>`, BIOS <version>. Power: <psu> — <how it is fed>; BIOS AC Recovery = On, so the box self-restarts after any power cut.
- OS: <distro + release>, kernel <version>. Networking: systemd-networkd via netplan (`/etc/netplan/99-server.yaml`, chmod 600). NTP: chrony. sshd socket-activated, key-only (`/etc/ssh/sshd_config.d/10-hardening.conf`). Docker Engine <ver>, Tailscale <ver>, cloudflared <ver>, gh <ver>.
- Purpose: always-on box operated by Claude Code sessions as user `<user>` — autonomous and long-running work in tmux; owner connects from laptop/phone (SSH over Tailscale, Claude Remote Control). Working root: `~/dev/`. This box's setup is the repo `~/dev/<control-repo>` (GitHub `<owner>/<control-repo>`, private): it carries autobox as `core/`, whose `core/bin/` is symlinked into `~/bin` and whose overlay `home/` is symlinked into `~`. Edit there, commit, push — `cc-publish` mirrors `core/` back to the public repo after every merge and refuses to publish anything that names this box.
- Reach it: Tailscale `ssh <user>@<tailscale-ip>` (MagicDNS `<box>.<tailnet>.ts.net`); Cloudflare tunnel `ssh <box>` → `<public-hostname>` (Access OTP); rescue cable `ssh <user>@<rescue-ip>` (laptop `<laptop-rescue-ip>`; static on `<iface>`, never remove); WiFi DHCP on `<hotspot-ssid>` or `<home-ssid>`. No monitor, no console. Details: `~/RUNBOOK.md`.
- Operating layer (deterministic scripts, `~/bin`): `cc` (sessions: `cc <repo>` planning, `cc <repo> <track>` worktree session, `--go` headless ralph-loop worker via `cc-loop`, `--say`, `handoff`, `resume`, `done`→PR, `digest`), `cc-board` (sidecar board `~/.cc/boards/`), `cc-checkpoint` (Stop hook: auto-commit+push only inside track worktrees), `cc-guard` (PreToolUse hook: owner gates for autonomous sessions and for member-facing ones, marked `.cc/member-facing`), `cc-notify` (Slack webhook + ntfy push to owner; secrets in `~/.cc/config`), `cc-msg`, `ccbox` (Docker sandbox, `~/ccbox/`), `box-status`, `cc-boot`/`cc-rc` + user service `tmux-main` (boot-time tmux `main` with a Remote-Control session named after the box), `cc-digest.timer` (daily 13:00 UTC), `cc-audit` + `cc-audit.timer` (daily 03:30 UTC, `auto` picks: 1st → monthly, Sunday → weekly, every 3rd day → `review`; modes `checks` mechanical / `review` LLM read of how well the box served the owner / `weekly`+`monthly` LLM code audits by area; all read-only, one Slack line + the box channel's canvas, reports in `~/.cc/state/audit/`, monthly findings → board track `audit-<yyyymm>`, never auto-fixes), `cc slack on|off|status|setup|channels|mkchannel|manifest` + `cc-slack` (two-way Slack on Claude Code channels: `#<repo>` ↔ that repo's session, DMs ↔ the box session; unit `cc-slackd`; app manifest in `core/slack/`). `cc-heartbeat` (10 s alive stamp → `~/.cc/state/power-events.log`, dates power losses), `cc-boot-notify` (Slack/ntfy message on every boot). Guide: `~/USAGE.md`; how to reach projects/agents (Slack/app/terminal): `~/COMMS.md`; design: `core/docs/DESIGN.md`.
- Conventions for Claude sessions here: planning sessions stay on the default branch and never auto-commit; track sessions never run git commit/push themselves (the hook does) and never merge; owner approval is required for merge to main/deploy, spend over budget, external comms in the owner's name, destructive or host/network/service changes. Keep replies concise and layered (shallow first); in Slack, structure with a few section bullets + sub-bullets (bullets organise, they don't pad; generally few, no hard cap when needed) — reference material goes to a doc/journal with a one-line link. Use subagents for large reads to protect context; journal to `~/.cc/state/<repo>/<track>/progress.md` before a handoff.
- Sudo: passwordless for `<user>`. Claude Code: `~/.claude/settings.json` (model `<model>`, effort <effort>; wide allow tier; ask tier = reboot/shutdown; hooks = cc-guard, cc-checkpoint). Owner key in `~/.ssh/authorized_keys`. GitHub CLI logged in as `<gh-user>`.
- Bootstrap state (<date>): sleep targets masked; unattended-upgrades security-only, no auto-reboot; installed tmux iw curl git build-essential htop rsync jq ripgrep fd-find smartmontools bubblewrap socat shellcheck. Logs: `/var/log/bootstrap/`.
- Out of scope without the owner's explicit go-ahead: ufw/nftables (never on the host), disk partitioning, reboot/shutdown, netplan writes while the box has one admin path. Never edit the interface the current session arrives on. Keep the hotspot SSID and the rescue address.
- Drive: <SMART baseline — power-on hours, TB written, wear, reallocated sectors>.
